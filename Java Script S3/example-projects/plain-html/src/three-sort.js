/*
 * @author Hong-Phuc Bui
 * initial version 27.12.2018
 * */

/* this file depends on two global variables Terminal and Board which are defined in dfhi.js  */

function main(...argv) {

    let array = [];
	for (let i = 0; i <= args.length; i++) {
        let n = parseFloat(argv[0]);
        array = array.push(n);
	}
}

